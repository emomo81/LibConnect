## 2025-01-01 - [Semantic Toggles]
**Learning:** Using a `div` as a toggle button breaks keyboard accessibility. Always use a semantic `<button type="button">`.
**Action:** Replace `div` or `span` toggles with `<button>` elements and ensure `aria-label` is present and updated.
